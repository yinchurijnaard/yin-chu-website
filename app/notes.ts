// Make sure everything follows proper convention

// Find a way to change the global style, for example:
// - Overall background colour
// - Overal text colour, such that I don't need to change the text colour locally. Rather, I'd like to have it more global

// Come up with an order of imports??? First "use client" if necessary? Then Next.js > React > Components > Dependencies???

// Set a baseline for padding, margin etc (review everywhere once to an almost 'finished' state)
