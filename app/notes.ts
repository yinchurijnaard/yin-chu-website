// Make sure everything follows proper convention

// Find a way to change the global style, for example:
// - Overall background colour (also for light mode etc (because dark mode will be the default))
// - Overal text colour, such that I don't need to change the text colour locally. Rather, I'd like to have it more global

// Come up with an order of imports??? First "use client" if necessary? Then Next.js > React > Components > Dependencies???

// Set a baseline for padding, margin etc (review everywhere once to an almost 'finished' state)

// Check font sizing and font size consistency!

// AT THE END OF THE WHOLE PROJECT: CHECK ALL FILES FOR ANY LEFTOVER COMMENTS/TO DO'S/CHECKS!!!

// Should I create a separte <hr> component?

// Even when on desktop, if the window is too small and therefore reaches mobile view, links (such as the navbar) should have a hover state, instead of a permanent underline (like it is on mobile)
